<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link  href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    #form{
      padding: 10px 10px;
      max-width: 350px;
      margin: 0 auto;
      background-color: aliceblue;
      box-shadow: 5px 5px rgb(162, 195, 224);
    }
    input{
   margin: 10px 10px;
    }
    h2{
      text-align: center;
      margin-bottom: 30px;
    }
    .main-in{
      display: flex;
      gap: 7px;
    }
  </style>
</head>
<h2>Register here</h2>
<body>
  <form method="post" class="row g-3 needs-validation" id="form" novalidate>
    <label for="regi_name" class="form-label">Name</label>
    <input type="text" name="name" id="regi_name" class="form-control" placeholder="firstname surname" maxlength="30" required>
     <label for="regi_email" class="form-label">Email</label>
    <input type="email" name="Email" placeholder="xyz@gmail.com" id="regi_email"  class="form-control" required>  
    <label for="regi_contact" class="form-label">Contact</label>
    <input type="text" min="10" max="10" pattern="[1-9]{1}[0-9]{9}" name="Contact" id="regi_contact"  class="form-control" required>
    <div class="main-in">
   <% if(locals.msg){%>
    <div style="color: red;"><%=msg%></div>
    <%}%>
    <div><a href="http://localhost:8050/api/login">Sign in</a></div>
  </div>
  <button type="submit" id="submit">Submit</button>
 

  </form>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>

(function () {
  'use strict'

  var forms = document.querySelectorAll('.needs-validation')

  Array.prototype.slice.call(forms)
    .forEach(function (form) {
      form.addEventListener('submit', function (event) {
        if (!form.checkValidity()) {
          event.preventDefault()
          event.stopPropagation()
        }

        form.classList.add('was-validated')
      }, false)
    })
})()
  </script>
</body>
</html>